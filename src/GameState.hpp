class GameState{
    private:
    public:
        void MOVE(){
        
        }

}

cin << command;
if command == 'MOVE'{
    cin << arg1 << arg2 << arg3;
    gameState.MOVE(arg1, arg2, arg3);
}
else if (command == 'SHOW'){
    gameState.SHOW();
}
else if ()




// MOVE I0 1 I1